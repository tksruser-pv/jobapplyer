[PATHS]

EXCEL_PATH = F:\TKSR PRODUCTION\job1\a1.xlsx

[API_KEYS]
# Your Groq API Key (required for LLM domain inference)
# CRITICAL: Replace 'PASTE_YOUR_NEW_SK_LIVE_KEY_HERE' with a newly generated key from Groq.
# The key must start with 'sk_live_' to work correctly with the OpenAI library.
GROQ_API_KEY = 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


[LLM_SETTINGS]
# The model used for domain inference
MODEL_NAME = llama-3.1-8b-instant

# The prompt used to strictly request the domain name
DOMAIN_INFERENCE_PROMPT = What is the official, current, and exact website domain name (only the domain, no https/www, no slashes, no dots except before the TLD) for the company '{company_name}'. Only return the domain name.

RESUME_SYSTEM_PROMPT = You are a world-class career coach. Your task is to rewrite a user  resume (provided as text) to perfectly match a given job description. Focus on: 1. Highlighting relevant skills and keywords from the job description. 2. Tailoring bullet points to match the required duties. 3. Maintaining the original tone and structure of the resume. Output ONLY the complete, rewritten resume text in Markdown format. DO NOT include any introductory or concluding remarks.

EMAIL_MODEL_NAME = llama-3.1-8b-instant
EMAIL_SYSTEM_PROMPT = You are an expert career consultant writing a personalized cover letter. Generate a professional cover letter suitable for email body format (no headers, footers, or subject line).
